1. When you do ls -l
	- the thing that priyanshu priyanshu written is owner and group of file/directory
2. ls -p flag will append '/' to all the directories, piping this result in grep -v / will help you list all the files in a particular directory
	(basically Q.1)
3. For file storage space taken up, du is the command
